const toggle=document.querySelector(".mobile-toggle");const nav=document.querySelector(".nav");if(toggle&&nav){toggle.addEventListener("click",()=>nav.classList.toggle("open"))}const revealItems=document.querySelectorAll(".reveal");const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")})},{threshold:.12});revealItems.forEach(item=>observer.observe(item));const FORMSPREE_ENDPOINT="";document.getElementById("enquiryForm").addEventListener("submit",async event=>{event.preventDefault();const payload={name:document.getElementById("name").value.trim(),email:document.getElementById("email").value.trim(),phone:document.getElementById("phone").value.trim(),requirement:document.getElementById("requirement").value.trim()};if(FORMSPREE_ENDPOINT){try{const response=await fetch(FORMSPREE_ENDPOINT,{method:"POST",headers:{"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify(payload)});if(response.ok){alert("Thanks. Your enquiry has been submitted.");event.target.reset();return}}catch(error){console.warn("Formspree failed. Falling back to mailto.",error)}}const subject=encodeURIComponent("Enterprise BI Demo Request");const body=encodeURIComponent(`Name: ${payload.name}\nEmail: ${payload.email}\nPhone: ${payload.phone}\nRequirement: ${payload.requirement}`);window.location.href=`mailto:dilip1100@gmail.com?subject=${subject}&body=${body}`});